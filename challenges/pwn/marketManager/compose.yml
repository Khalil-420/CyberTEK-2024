version: '3'
services:
  deploy:
    container_name: marketManager
    restart: unless-stopped
    build:
      context: ./challenge
      dockerfile: Dockerfile
    ports:
      - "1338:5000"
    privileged: true 
